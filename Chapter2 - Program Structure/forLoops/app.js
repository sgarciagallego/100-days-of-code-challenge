let number = 0;

// ! earlier we saw this code which prints even numbers until 12

while (number <= 12) {
  console.log(number);
  number = number + 2;
}

// output -> 0 -> 2 -> 4... -> 12

/* 
  * the for loop groups together all the statements related to the "state" of the loop
*/