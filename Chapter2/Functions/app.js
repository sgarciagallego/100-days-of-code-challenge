// * A functions is a piece of program wrapped in a value

prompt("Password:");    // example

// * executing a function is "invoking", "calling", or "applying".
// in most cases you will directly use the name of the variable which holds the value

// * values given to functions are called "arguments"